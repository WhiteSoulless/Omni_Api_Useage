<Window x:Class="OmniKeyStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:OmniKeyStudio.Views"
        xmlns:viewmodels="clr-namespace:OmniKeyStudio.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        Title="OmniKey AI Studio - Akıllı API Anahtar Yöneticisi &amp; Hızlı Model Merkezi" 
        Height="780" Width="1160" MinHeight="680" MinWidth="980"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundDarkBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Header Navigation Bar -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceDarkBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="0,0,0,1" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- App Branding -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="38" Height="38" CornerRadius="10" Background="{StaticResource PrimaryAccentBrush}" Margin="0,0,12,0">
                        <TextBlock Text="⚡" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="OmniKey AI Studio" FontWeight="Bold" FontSize="17" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Akıllı Anahtar Tespiti • Ücretsiz Hızlı Modeller • Kod Kasası" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Navigation Tabs -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <RadioButton Content="🔑 Anahtar Yöneticisi" Style="{StaticResource NavTabButton}"
                                 IsChecked="{Binding SelectedTabIndex, Converter={x:Null}, Mode=OneWay}"
                                 Click="NavKeyManager_Click"
                                 x:Name="TabBtnKeys"/>

                    <RadioButton Content="💬 Evrensel Sohbet" Style="{StaticResource NavTabButton}"
                                 Click="NavChat_Click"
                                 x:Name="TabBtnChat"/>

                    <RadioButton Content="⚡ Ücretsiz Hızlı Modeller" Style="{StaticResource NavTabButton}"
                                 Click="NavFreeModels_Click"
                                 x:Name="TabBtnFreeModels"/>

                    <RadioButton Content="📜 Kod &amp; Snippet Kasası" Style="{StaticResource NavTabButton}"
                                 Click="NavCodeVault_Click"
                                 x:Name="TabBtnCodeVault"/>
                </StackPanel>

                <!-- Right Security Badge -->
                <Border Grid.Column="2" Background="#162238" BorderBrush="#2D4373" BorderThickness="1" CornerRadius="6" Padding="10,6" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔒 DPAPI Korumalı" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource InfoBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Body / Content Pages (TabControl without headers) -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}" BorderThickness="0" Background="Transparent" Padding="0">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <!-- Tab 0: Key Manager -->
            <TabItem>
                <views:KeyManagerView DataContext="{Binding KeyManager}"/>
            </TabItem>

            <!-- Tab 1: Chat Playground -->
            <TabItem>
                <views:ChatPlaygroundView DataContext="{Binding ChatPlayground}"/>
            </TabItem>

            <!-- Tab 2: Free Models Directory -->
            <TabItem>
                <views:FreeModelsView DataContext="{Binding FreeModels}"/>
            </TabItem>

            <!-- Tab 3: Code Vault -->
            <TabItem>
                <views:CodeVaultView DataContext="{Binding CodeVault}"/>
            </TabItem>
        </TabControl>

        <!-- Bottom Global Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceDarkBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="0,1,0,0" Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding GlobalStatus}" 
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1" Text="OmniKey v1.0 • WPF .NET 9" 
                           Foreground="{StaticResource TextMutedBrush}" FontSize="11" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
